-- ============================================================
-- 全局配置
-- ============================================================
return {
    -- 自动刷新间隔（毫秒）
    refresh_interval = 2000,

    -- 启动时默认监控的端口
    default_port = 8080,

    -- 主窗口尺寸（像素），IUP 单位
    window_width  = 700,
    window_height = 480,
}
